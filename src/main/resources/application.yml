server:
  port: 9090
spring:
  datasource:
    username: root
    password: 2461.Atay
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/schoolAutomationSystem?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: schoolAutomationSystem